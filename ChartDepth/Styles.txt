================================================================================
FILE: App.xaml
================================================================================

<Application x:Class="ChartDepth.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Load in order: Colors first, then everything else -->
                <ResourceDictionary Source="Styles/Colors.xaml"/>
                <ResourceDictionary Source="Styles/ButtonStyles.xaml"/>
                <ResourceDictionary Source="Styles/DataGridStyles.xaml"/>
                <ResourceDictionary Source="Styles/ControlStyles.xaml"/>
                <ResourceDictionary Source="Styles/ChartStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>


================================================================================
FILE: Styles/Colors.xaml
================================================================================

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== MODERN COLOR PALETTE ===== -->
    
    <!-- Background Colors -->
    <Color x:Key="BgDark">#0A0E1A</Color>
    <Color x:Key="BgPanel">#111827</Color>
    <Color x:Key="BgPanelLight">#1A202E</Color>
    <Color x:Key="BgHover">#1E293B</Color>
    
    <!-- Accent Colors -->
    <Color x:Key="AccentBlue">#3B82F6</Color>
    <Color x:Key="AccentBlueLight">#60A5FA</Color>
    <Color x:Key="AccentBlueDark">#2563EB</Color>
    <Color x:Key="AccentCyan">#06B6D4</Color>
    
    <!-- Text Colors -->
    <Color x:Key="TextPrimary">#F1F5F9</Color>
    <Color x:Key="TextSecondary">#CBD5E1</Color>
    <Color x:Key="TextDim">#64748B</Color>
    
    <!-- UI Element Colors -->
    <Color x:Key="GridLines">#1E293B</Color>
    <Color x:Key="BorderSubtle">#334155</Color>
    
    <!-- Status Colors -->
    <Color x:Key="SuccessGreen">#10B981</Color>
    <Color x:Key="ErrorRed">#EF4444</Color>
    <Color x:Key="WarningYellow">#F59E0B</Color>
    
    <!-- Trading Colors -->
    <Color x:Key="AskRed">#EF4444</Color>
    <Color x:Key="AskRedDim">#DC2626</Color>
    <Color x:Key="BidGreen">#10B981</Color>
    <Color x:Key="BidGreenDim">#059669</Color>
    
    <!-- ===== SOLID COLOR BRUSHES ===== -->
    
    <!-- Background Brushes -->
    <SolidColorBrush x:Key="BgDarkBrush" Color="{StaticResource BgDark}" />
    <SolidColorBrush x:Key="BgPanelBrush" Color="{StaticResource BgPanel}" />
    <SolidColorBrush x:Key="BgPanelLightBrush" Color="{StaticResource BgPanelLight}" />
    <SolidColorBrush x:Key="BgHoverBrush" Color="{StaticResource BgHover}" />
    
    <!-- Accent Brushes -->
    <SolidColorBrush x:Key="AccentBlueBrush" Color="{StaticResource AccentBlue}" />
    <SolidColorBrush x:Key="AccentBlueLightBrush" Color="{StaticResource AccentBlueLight}" />
    <SolidColorBrush x:Key="AccentBlueDarkBrush" Color="{StaticResource AccentBlueDark}" />
    <SolidColorBrush x:Key="AccentCyanBrush" Color="{StaticResource AccentCyan}" />
    
    <!-- Text Brushes -->
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}" />
    <SolidColorBrush x:Key="TextDimBrush" Color="{StaticResource TextDim}" />
    
    <!-- UI Element Brushes -->
    <SolidColorBrush x:Key="GridLinesBrush" Color="{StaticResource GridLines}" />
    <SolidColorBrush x:Key="BorderSubtleBrush" Color="{StaticResource BorderSubtle}" />
    
    <!-- Status Brushes -->
    <SolidColorBrush x:Key="SuccessGreenBrush" Color="{StaticResource SuccessGreen}" />
    <SolidColorBrush x:Key="ErrorRedBrush" Color="{StaticResource ErrorRed}" />
    <SolidColorBrush x:Key="WarningYellowBrush" Color="{StaticResource WarningYellow}" />
    
    <!-- Trading Brushes -->
    <SolidColorBrush x:Key="AskRedBrush" Color="{StaticResource AskRed}" />
    <SolidColorBrush x:Key="AskRedDimBrush" Color="{StaticResource AskRedDim}" />
    <SolidColorBrush x:Key="BidGreenBrush" Color="{StaticResource BidGreen}" />
    <SolidColorBrush x:Key="BidGreenDimBrush" Color="{StaticResource BidGreenDim}" />

</ResourceDictionary>


================================================================================
FILE: Styles/ButtonStyles.xaml
================================================================================

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== NAVIGATION BUTTON STYLE ===== -->
    <Style x:Key="NavButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="20,14"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontFamily" Value="Segoe UI, Arial"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Margin="8,4">
                        <Grid>
                            <!-- Active indicator bar -->
                            <Border x:Name="activeBar"
                                    Width="4"
                                    HorizontalAlignment="Left"
                                    CornerRadius="0,2,2,0"
                                    Background="{StaticResource AccentBlueBrush}"
                                    Opacity="0"/>

                            <ContentPresenter x:Name="contentPresenter"
                                            Margin="{TemplateBinding Padding}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"
                                            TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Hover State -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource BgHoverBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                        </Trigger>

                        <!-- Pressed/Active State -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{StaticResource AccentBlue}" Opacity="0.15"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="{StaticResource AccentBlueLightBrush}"/>
                            <Setter TargetName="activeBar" Property="Opacity" Value="1"/>
                        </Trigger>

                        <!-- Active State (use Tag="Active" on button) -->
                        <Trigger Property="Tag" Value="Active">
                            <Setter TargetName="border" Property="Background">
                                <Setter.Value>
                                    <SolidColorBrush Color="{StaticResource AccentBlue}" Opacity="0.15"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="{StaticResource AccentBlueLightBrush}"/>
                            <Setter TargetName="activeBar" Property="Opacity" Value="1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== SETTINGS BUTTON STYLE ===== -->
    <Style x:Key="SettingsButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BgPanelBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="Padding" Value="16,10"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <StackPanel Orientation="Horizontal">
                            <!-- Settings Icon -->
                            <TextBlock Text="⚙" 
                                      FontSize="16" 
                                      Margin="0,0,8,0"
                                      VerticalAlignment="Center"
                                      Foreground="{TemplateBinding Foreground}"/>
                            <ContentPresenter VerticalAlignment="Center"
                                            TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource BgHoverBrush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBlueBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource BgPanelLightBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource AccentBlueBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>


================================================================================
FILE: Styles/DataGridStyles.xaml
================================================================================

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== DATAGRID BASE STYLE ===== -->
    <Style x:Key="BaseDataGridStyle" TargetType="DataGrid">
        <Setter Property="Background" Value="{StaticResource BgPanelBrush}" />
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="RowBackground" Value="Transparent" />
        <Setter Property="AlternatingRowBackground" Value="#00000000"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="RowHeight" Value="32"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="CanUserResizeRows" Value="False"/>
        <Setter Property="SelectionMode" Value="Single"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="IsReadOnly" Value="True"/>
    </Style>

    <!-- Default DataGrid Style -->
    <Style TargetType="DataGrid" BasedOn="{StaticResource BaseDataGridStyle}" />

    <!-- ===== DATAGRID COLUMN HEADER STYLE ===== -->
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{StaticResource BgPanelLightBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="BorderThickness" Value="0,0,0,2"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Height" Value="36"/>
    </Style>

    <!-- ===== DATAGRID ROW STYLE ===== -->
    <Style TargetType="DataGridRow">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Margin" Value="0"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource BgHoverBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background">
                    <Setter.Value>
                        <SolidColorBrush Color="#1E40AF" Opacity="0.2"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsSelected" Value="True"/>
                    <Condition Property="IsMouseOver" Value="True"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background">
                    <Setter.Value>
                        <SolidColorBrush Color="#1E40AF" Opacity="0.4"/>
                    </Setter.Value>
                </Setter>
            </MultiTrigger>
        </Style.Triggers>
    </Style>

    <!-- ===== DATAGRID CELL STYLE ===== -->
    <Style TargetType="DataGridCell">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="DataGridCell">
                    <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ===== DATAGRID TEXT COLUMN STYLES ===== -->
    
    <!-- Primary Text Style (for important columns like Ticker) -->
    <Style x:Key="DataGridPrimaryTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontFamily" Value="Consolas, Courier New"/>
    </Style>

    <!-- Secondary Text Style (for regular data) -->
    <Style x:Key="DataGridSecondaryTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="FontFamily" Value="Consolas, Courier New"/>
    </Style>

    <!-- Monospace Text Style (for dates, numbers) -->
    <Style x:Key="DataGridMonospaceTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="FontFamily" Value="Consolas, Courier New"/>
        <Setter Property="FontSize" Value="12"/>
    </Style>

    <!-- Price Text Style (for prices with accent color) -->
    <Style x:Key="DataGridPriceTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource AccentBlueBrush}"/>
        <Setter Property="FontFamily" Value="Consolas, Courier New"/>
        <Setter Property="FontWeight" Value="Medium"/>
    </Style>

    <!-- Ask Price Style -->
    <Style x:Key="DataGridAskPriceStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource AskRedBrush}"/>
        <Setter Property="FontFamily" Value="Consolas, Courier New"/>
        <Setter Property="FontWeight" Value="Medium"/>
    </Style>

    <!-- Bid Price Style -->
    <Style x:Key="DataGridBidPriceStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource BidGreenBrush}"/>
        <Setter Property="FontFamily" Value="Consolas, Courier New"/>
        <Setter Property="FontWeight" Value="Medium"/>
    </Style>

    <!-- PNL Text Style -->
    <Style x:Key="DataGridPnlTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Consolas, Courier New"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

</ResourceDictionary>


================================================================================
FILE: Styles/ControlStyles.xaml
================================================================================

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== USERCONTROL BASE STYLE ===== -->
    <Style TargetType="UserControl">
        <Setter Property="Background" Value="{StaticResource BgDarkBrush}" />
        <Setter Property="TextElement.Foreground" Value="{StaticResource TextPrimaryBrush}" />
        <Setter Property="FontFamily" Value="Segoe UI, Arial"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

    <!-- ===== CANVAS STYLE ===== -->
    <Style TargetType="Canvas">
        <Setter Property="Background" Value="{StaticResource BgPanelBrush}"/>
    </Style>

    <!-- ===== PANEL CONTAINER STYLE ===== -->
    <Style x:Key="PanelContainerStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BgPanelBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Padding" Value="0"/>
    </Style>

    <!-- ===== SECTION HEADER STYLE ===== -->
    <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="Margin" Value="0,0,0,12"/>
    </Style>

    <!-- ===== PANEL HEADER STYLE ===== -->
    <Style x:Key="PanelHeaderStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BgPanelLightBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="12,10"/>
        <Setter Property="CornerRadius" Value="8,8,0,0"/>
    </Style>

    <!-- ===== TEXTBLOCK STYLES ===== -->
    
    <!-- Default TextBlock for Canvas -->
    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="FontFamily" Value="Consolas, Courier New"/>
    </Style>

    <!-- Primary Text -->
    <Style x:Key="PrimaryTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Normal"/>
    </Style>

    <!-- Secondary Text -->
    <Style x:Key="SecondaryTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <!-- Dim Text -->
    <Style x:Key="DimTextStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource TextDimBrush}"/>
        <Setter Property="FontSize" Value="11"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <!-- Monospace Text -->
    <Style x:Key="MonospaceTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Consolas, Courier New"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
    </Style>

</ResourceDictionary>


================================================================================
FILE: Styles/ChartStyles.xaml
================================================================================

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== ASK RECTANGLE STYLE ===== -->
    <Style x:Key="AskRectangleStyle" TargetType="Rectangle">
        <Setter Property="Fill">
            <Setter.Value>
                <SolidColorBrush Color="{StaticResource AskRedDim}" Opacity="0.25"/>
            </Setter.Value>
        </Setter>
        <Setter Property="StrokeThickness" Value="0"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Fill">
                    <Setter.Value>
                        <SolidColorBrush Color="{StaticResource AskRed}" Opacity="0.5"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ===== BID RECTANGLE STYLE ===== -->
    <Style x:Key="BidRectangleStyle" TargetType="Rectangle">
        <Setter Property="Fill">
            <Setter.Value>
                <SolidColorBrush Color="{StaticResource BidGreenDim}" Opacity="0.25"/>
            </Setter.Value>
        </Setter>
        <Setter Property="StrokeThickness" Value="0"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Fill">
                    <Setter.Value>
                        <SolidColorBrush Color="{StaticResource BidGreen}" Opacity="0.5"/>
                    </Setter.Value>
                </Setter>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>


================================================================================
FILE: MainWindow.xaml
================================================================================

<Window x:Class="ChartDepth.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:ChartDepth.Models"
        xmlns:viewModels="clr-namespace:ChartDepth.ViewModels"
        xmlns:views="clr-namespace:ChartDepth.Views"
        xmlns:local="clr-namespace:ChartDepth"
        mc:Ignorable="d"
        Title="Chart Depth - Order Book" 
        Height="600" 
        Width="1200"
        MinHeight="500"
        MinWidth="900"
        Background="{StaticResource BgDarkBrush}">

    <Window.DataContext>
        <viewModels:ChartViewModel />
    </Window.DataContext>

    <Grid Background="{StaticResource BgDarkBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- ===== TOP BAR ===== -->
        <Border Grid.Row="0" 
                Grid.Column="0" 
                Grid.ColumnSpan="2" 
                Background="{StaticResource BgPanelBrush}"
                BorderBrush="{StaticResource BorderSubtleBrush}"
                BorderThickness="0,0,0,1">
            <Grid Height="48">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- App Title -->
                <StackPanel Orientation="Horizontal" 
                           VerticalAlignment="Center" 
                           Margin="20,0,0,0">
                    <TextBlock Text="Chart Depth" 
                              FontSize="16" 
                              FontWeight="SemiBold"
                              Foreground="{StaticResource TextPrimaryBrush}"
                              VerticalAlignment="Center"/>
                    <Border Background="{StaticResource AccentBlueBrush}"
                           CornerRadius="4"
                           Padding="6,2"
                           Margin="12,0,0,0"
                           VerticalAlignment="Center">
                        <!-- Optional: Add version or beta tag here -->
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ===== LEFT SIDEBAR ===== -->
        <Border Grid.Row="1" 
                Grid.Column="0" 
                Background="{StaticResource BgPanelBrush}"
                BorderBrush="{StaticResource BorderSubtleBrush}"
                BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="NAVIGATION" 
                          Style="{StaticResource DimTextStyle}"
                          Margin="20,20,20,10"/>

                <StackPanel Grid.Row="1" Margin="0,10,0,0">
                    <Button Content="Order Book" 
                           Style="{StaticResource NavButtonStyle}"
                           Tag="Active"/>
                    <Button Content="My Orders" 
                           Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Trades" 
                           Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Analytics" 
                           Style="{StaticResource NavButtonStyle}"/>
                    <Button Content="Alerts" 
                           Style="{StaticResource NavButtonStyle}"/>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="20,0,20,20">
                    <Border Background="{StaticResource BgPanelLightBrush}"
                           BorderBrush="{StaticResource BorderSubtleBrush}"
                           BorderThickness="1"
                           CornerRadius="8"
                           Padding="12">
                        <StackPanel>
                            <TextBlock Text="Market Status" 
                                      Style="{StaticResource DimTextStyle}"
                                      Margin="0,0,0,8"/>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="8" Height="8" 
                                        Fill="{StaticResource SuccessGreenBrush}" 
                                        Margin="0,0,8,0"/>
                                <TextBlock Text="Connected" 
                                          Style="{StaticResource SecondaryTextStyle}"
                                          FontSize="12"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ===== MAIN CONTENT AREA ===== -->
        <Border Grid.Row="1" 
                Grid.Column="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Top Action Bar -->
                <Border Background="{StaticResource BgPanelBrush}"
                       BorderBrush="{StaticResource BorderSubtleBrush}"
                       BorderThickness="0,0,0,1"
                       Padding="16,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <!-- Add any additional controls here -->
                        </StackPanel>

                        <!-- Settings Button -->
                        <Button Content="Settings" 
                               Grid.Column="1"
                               Style="{StaticResource SettingsButtonStyle}"/>
                    </Grid>
                </Border>

                <!-- Main Content Area -->
                <views:ChartView Grid.Row="1"/>
            </Grid>
        </Border>
    </Grid>
</Window>


================================================================================
FILE: Views/ChartView.xaml
================================================================================

<UserControl x:Class="ChartDepth.Views.ChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converter="clr-namespace:ChartDepth.Converters"
             xmlns:local="clr-namespace:ChartDepth.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <converter:PercentageConverter x:Key="PercentageConverter" />
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- TOP LEFT: Bids and Asks Side by Side -->
        <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource PanelContainerStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Bids Table -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Border Grid.Row="0" Style="{StaticResource PanelHeaderStyle}">
                        <TextBlock Text="BIDS" 
                                   Foreground="{StaticResource BidGreenBrush}" 
                                   FontWeight="Bold" 
                                   FontSize="12"
                                   VerticalAlignment="Center"/>
                    </Border>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Bids}" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="PRICE" Binding="{Binding Price}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridBidPriceStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="VOLUME" Binding="{Binding Volume}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridSecondaryTextStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>

                <!-- Separator -->
                <Border Grid.Column="0" Width="1" HorizontalAlignment="Right" 
                        Background="{StaticResource BorderSubtleBrush}"/>

                <!-- Asks Table -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Border Grid.Row="0" Style="{StaticResource PanelHeaderStyle}">
                        <TextBlock Text="ASKS" 
                                   Foreground="{StaticResource AskRedBrush}" 
                                   FontWeight="Bold" 
                                   FontSize="12"
                                   VerticalAlignment="Center"/>
                    </Border>

                    <DataGrid Grid.Row="1" ItemsSource="{Binding Asks}" BorderThickness="0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="PRICE" Binding="{Binding Price}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridAskPriceStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="VOLUME" Binding="{Binding Volume}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridSecondaryTextStyle}"/>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
        </Border>

        <!-- BOTTOM LEFT: Vertical Order Book -->
        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource PanelContainerStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Style="{StaticResource PanelHeaderStyle}">
                    <TextBlock Text="ORDER BOOK" 
                               Foreground="{StaticResource AccentCyanBrush}" 
                               FontWeight="Bold" 
                               FontSize="12"
                               VerticalAlignment="Center"/>
                </Border>

                <DataGrid Grid.Row="1" ItemsSource="{Binding VerticalBook}" BorderThickness="0">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="PRICE" Binding="{Binding Price}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridPrimaryTextStyle}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="ASK QTY" Binding="{Binding AskQuantity}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridAskPriceStyle}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="BID QTY" Binding="{Binding BidQuantity}" Width="*">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridBidPriceStyle}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- RIGHT: Depth Chart Visualization -->
        <Border Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Style="{StaticResource PanelContainerStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Style="{StaticResource PanelHeaderStyle}">
                    <TextBlock Text="DEPTH CHART" 
                               Foreground="{StaticResource AccentBlueBrush}" 
                               FontWeight="Bold" 
                               FontSize="12"
                               VerticalAlignment="Center"/>
                </Border>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <!-- Price Labels -->
                    <Canvas x:Name="priceCanvas" Grid.Column="0" 
                            HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                            Background="{StaticResource BgPanelLightBrush}">
                        <ItemsControl ItemsSource="{Binding ChartPrices}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas Width="{Binding ActualWidth, ElementName=priceCanvas}" 
                                            HorizontalAlignment="Stretch" 
                                            VerticalAlignment="Stretch"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Price}" 
                                               Foreground="{StaticResource TextDimBrush}"
                                               FontSize="10"
                                               FontFamily="Consolas, Courier New"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Canvas.Top">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource PercentageConverter}">
                                                <Binding ElementName="priceCanvas" Path="ActualHeight" />
                                                <Binding Path="Offset" />
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Canvas.Left" Value="8"/>
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                        </ItemsControl>
                    </Canvas>

                    <!-- Vertical Separator -->
                    <Border Grid.Column="0" Width="1" HorizontalAlignment="Right" 
                            Background="{StaticResource BorderSubtleBrush}"/>

                    <!-- Ask Rectangles (Left side) -->
                    <Canvas x:Name="canvas" Grid.Column="1" 
                            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <ItemsControl ItemsSource="{Binding AskRectangles}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas Width="{Binding ActualWidth, ElementName=canvas}" 
                                            HorizontalAlignment="Stretch" 
                                            VerticalAlignment="Stretch"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Rectangle Style="{StaticResource AskRectangleStyle}">
                                        <Rectangle.Height>
                                            <MultiBinding Converter="{StaticResource PercentageConverter}">
                                                <Binding ElementName="canvas" Path="ActualHeight" />
                                                <Binding Path="Height" />
                                            </MultiBinding>
                                        </Rectangle.Height>
                                        <Rectangle.Width>
                                            <MultiBinding Converter="{StaticResource PercentageConverter}">
                                                <Binding ElementName="canvas" Path="ActualWidth"/>
                                                <Binding Path="Quantity" />
                                            </MultiBinding>
                                        </Rectangle.Width>
                                    </Rectangle>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Canvas.Top">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource PercentageConverter}">
                                                <Binding ElementName="canvas" Path="ActualHeight" />
                                                <Binding Path="Offset" />
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Canvas.Right" Value="0"/>
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                        </ItemsControl>
                    </Canvas>

                    <!-- Center Separator -->
                    <Border Grid.Column="1" Width="2" HorizontalAlignment="Right" 
                            Background="{StaticResource BorderSubtleBrush}"/>

                    <!-- Bid Rectangles (Right side) -->
                    <Canvas x:Name="canvas2" Grid.Column="2" 
                            HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                        <ItemsControl ItemsSource="{Binding BidRectangles}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas Width="{Binding ActualWidth, ElementName=canvas2}" 
                                            HorizontalAlignment="Stretch" 
                                            VerticalAlignment="Stretch"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Rectangle Style="{StaticResource BidRectangleStyle}">
                                        <Rectangle.Height>
                                            <MultiBinding Converter="{StaticResource PercentageConverter}">
                                                <Binding ElementName="canvas2" Path="ActualHeight" />
                                                <Binding Path="Height" />
                                            </MultiBinding>
                                        </Rectangle.Height>
                                        <Rectangle.Width>
                                            <MultiBinding Converter="{StaticResource PercentageConverter}">
                                                <Binding ElementName="canvas2" Path="ActualWidth"/>
                                                <Binding Path="Quantity" />
                                            </MultiBinding>
                                        </Rectangle.Width>
                                    </Rectangle>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Canvas.Top">
                                        <Setter.Value>
                                            <MultiBinding Converter="{StaticResource PercentageConverter}">
                                                <Binding ElementName="canvas2" Path="ActualHeight" />
                                                <Binding Path="Offset" />
                                            </MultiBinding>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="Canvas.Left" Value="0"/>
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                        </ItemsControl>
                    </Canvas>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>


================================================================================
FILE: Views/Trades.xaml
================================================================================

<UserControl x:Class="ChartDepth.Views.Trades"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChartDepth.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="12" Background="{StaticResource BgDarkBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- ===== PAST TRADES PANEL ===== -->
        <Border Grid.Column="0" Style="{StaticResource PanelContainerStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Style="{StaticResource PanelHeaderStyle}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📊" 
                                  FontSize="18" 
                                  VerticalAlignment="Center"
                                  Margin="0,0,10,0"/>
                        <TextBlock Text="Past Trades" 
                                  Style="{StaticResource SectionHeaderStyle}"
                                  Margin="0"
                                  VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding PastTrades}" BorderThickness="0">
                    <DataGrid.Columns>
                        <!-- Date Column -->
                        <DataGridTextColumn Header="DATE" Binding="{Binding Date}" Width="Auto" MinWidth="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridMonospaceTextStyle}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Ticker Column -->
                        <DataGridTextColumn Header="TICKER" Binding="{Binding Ticker}" Width="Auto" MinWidth="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridPrimaryTextStyle}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Price Column -->
                        <DataGridTextColumn Header="PRICE" Binding="{Binding Price, StringFormat={}{0:N2}}" Width="Auto" MinWidth="90">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridPriceTextStyle}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Volume Column -->
                        <DataGridTextColumn Header="VOLUME" Binding="{Binding Volume}" Width="Auto" MinWidth="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridSecondaryTextStyle}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Side Column with Pill -->
                        <DataGridTemplateColumn Header="SIDE" Width="Auto" MinWidth="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Padding="10,4" HorizontalAlignment="Left" MinWidth="60">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Side}" Value="BUY">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <SolidColorBrush Color="{StaticResource SuccessGreen}" Opacity="0.15"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="BorderBrush" Value="{StaticResource SuccessGreenBrush}"/>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Side}" Value="SELL">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <SolidColorBrush Color="{StaticResource ErrorRed}" Opacity="0.15"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="BorderBrush" Value="{StaticResource ErrorRedBrush}"/>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Side}" 
                                                  FontWeight="SemiBold" 
                                                  FontSize="11"
                                                  HorizontalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Side}" Value="BUY">
                                                            <Setter Property="Foreground" Value="{StaticResource SuccessGreenBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Side}" Value="SELL">
                                                            <Setter Property="Foreground" Value="{StaticResource ErrorRedBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Error Column with Pill -->
                        <DataGridTemplateColumn Header="ERROR" Width="*" MinWidth="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Padding="10,4" HorizontalAlignment="Left" MaxWidth="200"
                                            BorderThickness="1">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background">
                                                    <Setter.Value>
                                                        <SolidColorBrush Color="{StaticResource ErrorRed}" Opacity="0.15"/>
                                                    </Setter.Value>
                                                </Setter>
                                                <Setter Property="BorderBrush" Value="{StaticResource ErrorRedBrush}"/>
                                                <Setter Property="Visibility">
                                                    <Setter.Value>
                                                        <Binding Path="Error">
                                                            <Binding.Converter>
                                                                <local:NullToVisibilityConverter/>
                                                            </Binding.Converter>
                                                        </Binding>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Error}" 
                                                  Foreground="{StaticResource ErrorRedBrush}"
                                                  FontSize="11"
                                                  FontWeight="Medium"
                                                  TextTrimming="CharacterEllipsis"
                                                  ToolTip="{Binding Error}"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- ===== OPEN POSITIONS PANEL ===== -->
        <Border Grid.Column="1" Style="{StaticResource PanelContainerStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Style="{StaticResource PanelHeaderStyle}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💼" 
                                  FontSize="18" 
                                  VerticalAlignment="Center"
                                  Margin="0,0,10,0"/>
                        <TextBlock Text="Open Positions" 
                                  Style="{StaticResource SectionHeaderStyle}"
                                  Margin="0"
                                  VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding OpenPositions}" BorderThickness="0">
                    <DataGrid.Columns>
                        <!-- Ticker Column -->
                        <DataGridTextColumn Header="TICKER" Binding="{Binding Ticker}" Width="Auto" MinWidth="90">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridPrimaryTextStyle}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Direction Column with Pill -->
                        <DataGridTemplateColumn Header="DIRECTION" Width="Auto" MinWidth="90">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Padding="10,4" HorizontalAlignment="Left" MinWidth="60">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Direction}" Value="LONG">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <SolidColorBrush Color="{StaticResource SuccessGreen}" Opacity="0.15"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="BorderBrush" Value="{StaticResource SuccessGreenBrush}"/>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Direction}" Value="SHORT">
                                                        <Setter Property="Background">
                                                            <Setter.Value>
                                                                <SolidColorBrush Color="{StaticResource ErrorRed}" Opacity="0.15"/>
                                                            </Setter.Value>
                                                        </Setter>
                                                        <Setter Property="BorderBrush" Value="{StaticResource ErrorRedBrush}"/>
                                                        <Setter Property="BorderThickness" Value="1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding Direction}" 
                                                  FontWeight="SemiBold" 
                                                  FontSize="11"
                                                  HorizontalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Direction}" Value="LONG">
                                                            <Setter Property="Foreground" Value="{StaticResource SuccessGreenBrush}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Direction}" Value="SHORT">
                                                            <Setter Property="Foreground" Value="{StaticResource ErrorRedBrush}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Side Column -->
                        <DataGridTextColumn Header="SIDE" Binding="{Binding Side}" Width="Auto" MinWidth="70">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridSecondaryTextStyle}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Volume Column -->
                        <DataGridTextColumn Header="VOLUME" Binding="{Binding Volume}" Width="Auto" MinWidth="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock" BasedOn="{StaticResource DataGridSecondaryTextStyle}"/>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- PNL Column with Color Coding -->
                        <DataGridTemplateColumn Header="PNL" Width="*" MinWidth="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding PNL, StringFormat={}{0:N2}}" 
                                              Style="{StaticResource DataGridPnlTextStyle}">
                                        <TextBlock.Foreground>
                                            <Binding Path="PNL">
                                                <Binding.Converter>
                                                    <local:PnlColorConverter/>
                                                </Binding.Converter>
                                            </Binding>
                                        </TextBlock.Foreground>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>


================================================================================
END OF FILES
================================================================================

FOLDER STRUCTURE:
================================================================================

YourProject/
├── App.xaml
├── MainWindow.xaml
├── Styles/
│   ├── Colors.xaml
│   ├── ButtonStyles.xaml
│   ├── DataGridStyles.xaml
│   ├── ControlStyles.xaml
│   └── ChartStyles.xaml
└── Views/
    ├── ChartView.xaml
    └── Trades.xaml

================================================================================
NOTES:
================================================================================

1. Create a "Styles" folder in your project root
2. Create a "Views" folder in your project root (if not already present)
3. Copy each file section above to its corresponding location
4. Build your project - all styles will be automatically available!

5. Make sure your existing converters are still in place:
   - PercentageConverter
   - NullToVisibilityConverter
   - PnlColorConverter

These converters are referenced in the XAML but need to be in your C# code.